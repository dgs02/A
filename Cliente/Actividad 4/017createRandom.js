'use strict';

function random(min, max) {
    return Math.random() * (max - min) + min;
  }
  
  // Ejemplos de uso
  alert(random(1, 5).toFixed(10)); // Ejemplo 1
  alert(random(1, 5).toFixed(10)); // Ejemplo 2
  alert(random(1, 5).toFixed(10)); // Ejemplo 3
  